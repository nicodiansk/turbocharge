---
description: "Load, save, or clear session context from .turbocharge/memory/"
disable-model-invocation: true
---

Use the session-memory skill.

If arguments contain 'load', focus on loading and displaying saved context.
If arguments contain 'save', focus on persisting current session state.
If arguments contain 'clear', focus on clearing memory with confirmation.
If no arguments, default to loading context.
